# Knowledge

Research notes, reference material, and task-specific context.

Use this folder for anything that doesn't belong in `Tasks/` (which tracks actionable work) or `SHARED-AI/` (which holds identity/context for AI assistants).
